version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "<ARN of ECS TaskDefinition>"
        LoadBalancerInfo:
          ContainerName: "sample-website"
          ContainerPort: 80
